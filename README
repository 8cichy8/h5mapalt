h5mapalt (MMH55_Mapmixer)

This tool can change homm5/mmh55 maps (swap creatures/artifacts and other).
Place it in your "bin" or "Maps" folder.

Dependencies:
    python3
    wxPython Phoenix [(snapshots)](https://wxpython.org/Phoenix/snapshot-builds/)  (pip3 install wxPython)

To build exe:
    pip3 install pyinstaller
    pyinstaller --onefile h5mapalt_gui.py

